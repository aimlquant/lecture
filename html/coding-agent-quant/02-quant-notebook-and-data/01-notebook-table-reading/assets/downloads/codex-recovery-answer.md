커널을 재시작하면 이전에 실행한 셀의 변수들이 사라지므로, `비교가격`이 정의되지 않았다는 오류가 발생했습니다.
앞쪽 셀에 코드가 남아 있어도 다시 실행하기 전에는 `기준가격`과 `비교가격`이 메모리에 저장되지 않습니다.
먼저 `기준가격 = 100`과 `비교가격 = 103`이 있는 셀을 실행한 다음, 가격 변화율을 계산하고 출력하는 셀을 실행하세요.
예상 출력은 `가격 변화: 3.0%`입니다.